# TrimReads 核心依赖
biopython>=1.79
numpy>=1.21
tqdm>=4.62

# 可选依赖 (用于高级功能)
matplotlib>=3.5   # 用于质量可视化
seaborn>=0.11     # 用于高级绘图
scipy>=1.7        # 用于统计分析
pandas>=1.3       # 用于数据处理

# 开发依赖 (非必需)
pytest>=6.2       # 用于单元测试
pytest-cov>=3.0   # 用于测试覆盖率
flake8>=4.0       # 用于代码检查
black>=22.3       # 用于代码格式化
twine>=4.0        # 用于包发布
sphinx>=5.0       # 用于文档生成
sphinx_rtd_theme>=1.0  # 文档主题
jupyter>=1.0      # 用于笔记本演示